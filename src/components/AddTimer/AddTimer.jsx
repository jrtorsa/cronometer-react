import React from 'react';
import './AddTimer.scss';

const AddTimer = () => {
  return (
    <div className="add-timer">
      <button className="add-timer-btn">
        <i class="far fa-plus-square add-timer-btn"></i>
      </button>
    </div>
  );
};

export default AddTimer;
